### BEGIN INIT INFO
# Provide:
# Required-Start:    $remote_fs $syslog
# Required-Stop:     $remote_fs
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description: Litelog-Sh Git-Annex Watcher
### END INIT INFO
. /etc/litelog
. "$LITELOGDIR"/sh/sysvinit/functions

cd "$LOGDIR"
cmd="git annex watch --foreground"

go "$@"
