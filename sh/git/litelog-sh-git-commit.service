[Unit]
Description=Litelog-Sh Git Committer

[Service]
EnvironmentFile=/etc/litelog
ExecStart=/bin/sh -c "git -C ${LOGDIR} add ."
ExecStart=/bin/sh -c "git -C ${LOGDIR} commit -m $$(date -Iseconds)"
Type=oneshot
